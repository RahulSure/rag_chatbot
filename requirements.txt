# Root dev/ingestion requirements for the RAG chatbot platform.
# For the API service, prefer platform/apps/api/requirements.txt (pinned, includes
# redis / celery / prometheus). This file covers local ingestion + running the API.

# FastAPI + server
fastapi
uvicorn[standard]
python-dotenv
python-multipart

# LlamaIndex core + integrations
llama-index
llama-index-vector-stores-mongodb
llama-index-embeddings-huggingface

# Embeddings
huggingface_hub
torch
sentence-transformers

# Vector store
pymongo

# Redis (session memory)
redis

# OCR pipeline (EasyOCR — no system tesseract needed)
easyocr
pymupdf
Pillow

# HTTP clients for Krutrim API
requests
httpx

# Utilities
pydantic

# Document parsing
llama-cloud
